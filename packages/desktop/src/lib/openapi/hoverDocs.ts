/**
 * Curated hover documentation for OpenAPI property keys. The implementation
 * was promoted to `@nouto/core` (shared with the VS Code hover provider); this
 * module re-exports it so desktop consumers keep their existing import path.
 */
export { resolveHoverDocs } from '@nouto/core/services/openapi/completion/hover';
export type { HoverDocsResult } from '@nouto/core/services/openapi/completion/hover';
