{
  // svelte-check config: test files carry stale fixture shapes (tracked as
  // follow-up debt) and vitest does not type-check them, so they are excluded
  // from the CI gate. Editors keep using tsconfig.json.
  "extends": "./tsconfig.json",
  "exclude": ["src/**/*.test.ts", "src/test/**"]
}
