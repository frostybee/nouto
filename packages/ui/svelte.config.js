// Read by svelte-check. The Vite build configures the Svelte plugin in
// vite.config.ts; svelte-check only needs the config file to exist. Components
// use plain TypeScript and CSS, so no preprocessor is required here.
export default {};
