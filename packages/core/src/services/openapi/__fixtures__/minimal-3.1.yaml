# Minimal valid OpenAPI 3.1.x document (YAML)
openapi: "3.1.0"
info:
  title: Minimal 3.1 API
  version: "1.0.0"
paths:
  /ping:
    get:
      summary: Ping
      operationId: ping
      responses:
        "200":
          description: OK
