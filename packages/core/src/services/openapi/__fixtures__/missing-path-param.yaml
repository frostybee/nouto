# Path template {id} has no matching in:path parameter declaration
openapi: "3.1.0"
info:
  title: Missing Path Param
  version: "1.0.0"
paths:
  /users/{id}:
    get:
      summary: Get user
      responses:
        "200":
          description: OK
