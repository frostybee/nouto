---
title: From cURL, OpenAPI, HAR & More
description: Import from Thunder Client, cURL, OpenAPI specs, HAR files, and URLs.
---

## Thunder Client

Use the command **Import Thunder Client** and select your Thunder Client data directory.

## cURL

Use the command **Import from cURL** and paste your cURL command. Nouto parses it into a request.

## OpenAPI / Swagger

Use the command **Import OpenAPI Specification** and select a `.json` or `.yaml` OpenAPI spec file. Nouto creates a collection with one request per endpoint.

## HAR Files

Use the command **Import from URL** or import HAR files directly to recreate requests from network recordings.

## Nouto Native Format

Use **Import Nouto Collection** to import collections previously exported in Nouto's own format. All IDs are regenerated on import to avoid conflicts.
