# pnpm configuration
# See: https://pnpm.io/npmrc
#
# As of pnpm 11 this file is read ONLY for registry and authentication
# settings. Every other pnpm setting lives in pnpm-workspace.yaml — adding
# them back here would silently have no effect.
